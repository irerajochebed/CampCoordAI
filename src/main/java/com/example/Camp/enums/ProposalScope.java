package com.example.Camp.enums;

public enum ProposalScope {
    DISTRICT, // Reviewed by District Pastor of target district
    FIELD,    // Reviewed by Field Leader of the target field
    UNION     // Reviewed by Dept Leader then escalated to Union Leader / Union Administrator
}
