package com.example.Camp.enums;

public enum ProposalScope {
    FIELD,   // Reviewed by Field Leader of the target field
    UNION    // Reviewed by Dept Leader then escalated to Union Administrator
}
